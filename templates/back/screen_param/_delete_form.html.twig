<form method="post" action="{{ path('app_back_screen_param_delete', {'id': screen_param.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ screen_param.id) }}">
    <button class="btn">Delete</button>
</form>
